  void deletei(Node* &head, int pos){
        if(pos==0){
            head=head->next;
            return;
        }
        Node* temp=head;
        for(int i=0;i<pos-1;i++){
            temp=temp->next;
        }
        temp->next=temp->next->next;
        
    }
